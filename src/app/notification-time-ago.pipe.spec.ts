import { NotificationTimeAgoPipe } from './notification-time-ago.pipe';

describe('NotificationTimeAgoPipe', () => {
  it('create an instance', () => {
    const pipe = new NotificationTimeAgoPipe();
    expect(pipe).toBeTruthy();
  });
});
